/*import { AppPage } from './Register.po';
import { browser, by, element } from 'protractor';
import { get } from 'http';

describe('workspace-project App', () => {
    let page: AppPage;
  
    beforeEach(() => {
      page = new AppPage();
    });
    it('should display sign up page with fill form',()=>{
        browser.sleep(2000);
        page.navigateTo();
       // expect(page.getemail())
       // expect(page.getPwd())
       expect(page.getfirstname())
       expect(page.getlastname())
       expect(page.getemailid())
       expect(page.getpassword())
       expect(page.getcpwd())

        browser.sleep(2000);
        expect(page.clicksubmitt())
        //page.navigateTo();
      });
});*/